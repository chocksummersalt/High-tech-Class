SELECT ename ||'''s sal is $' || Sal "NAME and SAL" 
FROM emp ;

SET pagesize 50

SELECT name ||' ''s ID :' || ID ||' WEIGHT is '|| weight

FROM student  

SELECT ENAME||'('||job||') , '||ENAME|| ''''||job||'''' "NAME AND JOB" 
FROM emp